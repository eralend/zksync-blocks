{
  "name": "zksync-era-blocks",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "b": "graph codegen && graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ graph-lending",
    "create-local": "graph create --node http://localhost:8020/ graph-lending",
    "remove-local": "graph remove --node http://localhost:8020/ graph-lending",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 graph-lending",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.51.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
